```python
car.sort() #对car按字母序进行排序

car.sort(reverse=True) #按字母逆序排序并输出结果(数字按从小到大排)

car.reverse() # 反转car中元素的排列顺序

sorted(car) #按字母序排序，但不改变car中元素的排列顺序(数字按从小到大排)

sorted(car,reverse=True)#按字母逆序排序
```

